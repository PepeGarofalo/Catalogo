// src/config/apiConfig.js
const API_BASE_URL = '';

const API_ENDPOINTS = {
  AUTH: `${API_BASE_URL}/auth`,
  INICIATIVA: `${API_BASE_URL}/iniciativa`,
  UPLOAD: `${API_BASE_URL}/uploads/upload`,
  // Puedes agregar más endpoints aquí según sea necesario
};

export default API_ENDPOINTS;
